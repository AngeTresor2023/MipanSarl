// Ce fichier est intentionnellement vide — le home page metadata est dans app/layout.tsx
export default function HomeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
